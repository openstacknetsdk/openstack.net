{
    "name": "TryStack",
    "description": "Valid TryStack credentials with the tenant specified by name",
    "vendor": "OpenStack",
    "baseAddress": "http://localhost:5000",
    "defaultRegion": "RegionOne",
    "authRequest": {
        "auth": {
            "tenantName": "simulated_tenant",
            "passwordCredentials": {
                "username": "simulated_user",
                "password": "simulated_password"
            }
        }
    }
}
